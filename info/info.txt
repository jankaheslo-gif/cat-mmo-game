The listeners are in, but position updates from tile parsing are unreliable.
Quick test fix — in client script, after the worldState listener, add this line inside the forEach:
JavaScriptremotePlayers[p.id].position.set(x, 1, z); // force y = 1 to see it
Then push and test.
If still not visible, the model replacement (replaceBoxWithCat) is failing because sharedCatModel is not shared properly.